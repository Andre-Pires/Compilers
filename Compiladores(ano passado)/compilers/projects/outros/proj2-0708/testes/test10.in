int sum;
int a[10];

typedef struct {
  int x;
  int y;
} Coord;

struct Coord* A;
struct Coord* B;

void initStruct(struct Coord *p){
  p->x = 0;
  p->y = 0;
}

void init(){

  i = 0; 
  while(i < 10){
    a[i] = 0;
    i = i + 1;
  }
  sum=0;

  initStruct(A);
  initStruct(B);
}

void f(int n){
  int m;
  m = n;
  while(sum > 0){
    sum  = sum + m;
  }
}

